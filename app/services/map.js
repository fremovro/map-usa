import Service from '@ember/service';
import { Evented } from './node-modules/leaflet/leaflet-src.js';
import { Class } from './node-modules/leaflet/leaflet-src.js';

export default class MapService extends Service {
  // map.on('click', function(e) {
  // 	alert(e.latlng);
  // } );
}
